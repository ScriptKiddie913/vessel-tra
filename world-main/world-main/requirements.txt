fastapi>=0.111.0
uvicorn>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9
# aiosqlite, pyais, websockets are only used for local ship-tracking features
# and are already guarded with try/except — not needed for Vercel deployment.
